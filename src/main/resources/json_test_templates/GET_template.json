{
  "scenarioName": "This is a sample scenario for GET requests",
  "steps": [
    {
      "name": "get_template",
      "description": "description here",
      "url": "url for operation. Base url should be in project.properties file",
      "method": "GET",
      "request": {
        "what to add here": "add request headers-replace key:what to add here, with header name. Leave request as blank if no headers"
      },
      "verify": {
        "status": "expected status code - number, no need of double quotes",
        "body": "Expected body should be in external json file. Mention path here. Eg:${JSON.FILE:foldername/filename.json}"
      }
    }
  ]
}
