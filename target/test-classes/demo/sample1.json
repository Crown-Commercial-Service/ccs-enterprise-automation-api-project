{
  "scenarioName": "Create the user and verify - 1",
  "steps": [
    {
      "name": "post-user",
      "url": "/users",
      "method": "POST",
      "request": {
        "body" : "${JSON.FILE:body/body1.json}"
      },
      "verify": {
        "status": 201
      }
    },
    {
      "name": "get-user",
      "url": "/users/${$.post-user.response.body.id}",
      "method": "GET",
      "request": {
      },
      "verify": {
        "status": 404
      }
    }
  ]
}